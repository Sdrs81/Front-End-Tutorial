
// 1. Aritmetik Operatörler

// + - / * %

// variable++, -- or ++variable       
// if data = variable++  ==> data is smaller than varibale
// if data = ++variable  ==> data is the same with variable


// 2. Atama Operatörleri

let data = 0;
let a = 5;
let b = 4;

veri = a;
veri += a; // veri = veri + a; 
veri -= a; // veri = veri - a;
veri *= a; // veri = veri * a;
veri /= a; // veri = veri / a;
veri %= a; // veri = veri % a;


// 3. Karşılaştırma Operatörleri

veri = a == b; // is equal?, that return true or false so veri is boolean value
// also doesn't check for types, so --> data = 5 == "5" is true for data

veri = a === b; //but in this check also types so --> data = 5 === "5" is false for data

// != !== < > <= >=

// x!==y is equal wit this !(x === y)


// 4. Mantıksal Operatörler

// && || !
